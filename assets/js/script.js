
let comment_container = document.getElementById('comment-container');

comment_container.addEventListener('click',function(e){
    let replyBtnClick = e.target.classList.contains('add-reoly');
    let submitBtnClic = e.target.classList.contains('submit-btn');

    if(replyBtnClick){

    }

    if(submitBtnClic){

    }
})
